# Archived Card Implementations

This folder keeps retired Python card implementations for reference while the active library is rebuilt.

Archived cards are not imported by `training_cards.cards` and should not be treated as active seed cards. If an archived card is reused, copy or rewrite it into the active level/profile folder and register it in the relevant level `__init__.py`.
